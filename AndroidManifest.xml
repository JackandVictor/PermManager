<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhntd.opsmanager"
    android:sharedUserId="android.uid.system"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="19"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.WRITE_SETTINGS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Settings" >

        <!-- dev tweak here -->
        <activity
            android:name="com.zhntd.opsmanager.OpsManager"
            android:excludeFromRecents="true"
            android:icon="@mipmap/ic_launcher_settings"
            android:label="@string/dev_tweak_title"
            android:taskAffinity="" >
            <intent-filter>
                <action android:name="com.zhntd.dev.TWEAK" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.zhntd.opsmanager.activity.OpsDetails"
            android:excludeFromRecents="true"
            android:icon="@mipmap/ic_launcher_settings"
            android:label="@string/title_apps"
            android:taskAffinity="" >
        </activity>
        <activity
            android:name="com.zhntd.opsmanager.activity.NetManagerActivity"
            android:label="@string/title_activity_net_manager" >
        </activity>
    </application>

</manifest>